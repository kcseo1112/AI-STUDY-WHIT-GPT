# 서버 전용 Python 패키지 의존성
# 서버에서만 설치하면 됩니다

# Supabase (데이터베이스)
supabase>=2.0.0

# OpenAI (AI 서비스 - 서버에서 키워드/요약 생성 시 사용 가능)
openai>=1.0.0

# 기타
python-dotenv>=1.0.0

# 주의사항:
# - STT 변환은 클라이언트에서 수행되지만, 서버에서도 OpenAI를 사용할 수 있습니다.
# - Supabase 연결 정보가 .env 파일에 설정되어 있어야 합니다.

