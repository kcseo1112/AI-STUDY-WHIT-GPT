# 클라이언트 전용 Python 패키지 의존성
# 클라이언트에서만 설치하면 됩니다

# OpenAI (AI 서비스 및 Whisper STT API)
# 클라이언트에서 STT 변환 및 키워드/요약 생성에 사용
openai>=1.0.0

# 기타
python-dotenv>=1.0.0

# 주의사항:
# - STT 변환은 클라이언트에서 OpenAI Whisper API를 직접 사용합니다.
# - OpenAI API 키가 .env 파일에 설정되어 있어야 합니다.
# - Supabase는 서버에서만 사용됩니다.

